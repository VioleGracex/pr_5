// elementType.ts
export interface ElementType {
    id: string; // Add a unique identifier for each element
    content: string; // Adjust based on your needs
    position: { x: number; y: number }; // Add position information
  }
  